---
- name: Ansible
  hosts: localhost
  become: true
  gather_facts: no
  tasks:
    - name: vault-decrypt
      command: 'ansible-vault decrypt key.json --vault-password-file pass'
      ignore_errors: True
