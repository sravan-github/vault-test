---
- name: Ansible
  hosts: localhost
  become: true
  gather_facts: no
  tasks:
    - name: vault-decrypt
      command: "ansible-vault decrypt key.json --vault-password-file pass --output key2.json"
      register: dfboot

    - debug: var=dfboot.stdout
